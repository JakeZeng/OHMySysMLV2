refactor(m12.4): 路由合并 + 旧 /models/:mid 跳转 + 删除 M11 dead code

路由：
- routes.tsx：移除 ModelEditor 路由，挂载 LegacyModelRedirect（自动跳 /projects/:pid）

清理（删除 9 文件）：
- pages/ModelEditor.tsx
- components/views/{ViewSidebar,ViewPropertiesDialog}.tsx + 整个 views/ 目录
- components/VersionHistoryPanel.tsx（仅 ModelEditor 用）
- services/modelApi.ts
- stores/viewStore.ts + viewStore.test.ts
- types/viewLegacy.ts
- App.tsx（POC v2 demo，未挂路由）

更新：
- modelStore：删除 loadModel/saveModel（Model 实体已不存在）、recentModelIds/addRecentModel、entityKind 'model' 分支；引入改为 packageApi + viewApi
- GlobalSearch：搜索项目+包+视图，命中跳 /projects/:pid?package=/?view=
- DashboardPage：移除「最近模型」区块 + modelCount
- ImportPage：result.model → result.package，链接改 /projects/:pid?package=
- ProjectList：项目导入走 packageApi.create（替代 modelApi.create）
- KeyboardShortcutsModal：补全工程树快捷键 + 改名「工程编辑器」
- packageApi / viewApi：新增 search(q, limit) 方法

typecheck + 133 vitest 全绿（删除 viewStore.test 后从 141 → 133，符合预期）。
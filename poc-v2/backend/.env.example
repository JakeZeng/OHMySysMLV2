# SysML v2 MBSE 后端环境变量
# 复制为 .env 并按需修改

# 服务端口（默认 8080）
PORT=8080

# SQLite 数据库路径（默认 ./sysmlv2.db）
DB_PATH=./sysmlv2.db

# Gin 运行模式（debug / release / test，默认 release）
GIN_MODE=release

# JWT 密钥（生产环境必须修改！）
JWT_SECRET=change-me-in-production

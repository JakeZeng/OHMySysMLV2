# SysMLv2 MBSE 系统 UI/UX 设计文档

> **版本**: v1.2（M12 工程树 + 三栏布局重构）  
> **日期**: 2026-09-24  
> **技术栈**: React 18 + TypeScript + Radix UI + Tailwind CSS  
> **目标用户**: 中小团队/个人工程师、教育培训、AI 自动化先行者  
> **M12 变更摘要**: 删除 `/models/:modelId` 独立编辑器路由；工程详情页改为三栏 IDE 布局（工程树 | 建模区 | 属性面板），与 SysML v2 一等实体语义对齐

---

## 目录

1. [设计原则与愿景](#1-设计原则与愿景)
2. [整体布局设计](#2-整体布局设计)
3. [核心视图设计](#3-核心视图设计)
4. [交互流程设计](#4-交互流程设计)
5. [组件体系](#5-组件体系)
6. [状态指示设计](#6-状态指示设计)
7. [快捷键设计](#7-快捷键设计)
8. [响应式策略](#8-响应式策略)
9. [视觉规范](#9-视觉规范)

---

## 1. 设计原则与愿景

### 1.1 核心设计原则

| 原则 | 描述 | 实现方式 |
|------|------|----------|
| **专业与直观** | 面向工程人员的专业工具，同时保持学习曲线平缓 | 类 VS Code 的专业感 + 清晰的引导 |
| **文本与图形融合** | SysML 天然支持文本/图形双视角 | 双模式无缝切换，保持状态同步 |
| **AI 增强** | AI 作为协作者而非替代者 | 随时可召唤的 AI 助手，生成建议可一键采纳 |
| **高效专注** | 减少干扰，聚焦核心建模工作 | 上下文面板、沉浸式编辑模式 |
| **渐进式披露** | 新手看到简洁界面，高级用户可深入 | 可折叠面板、可配置的复杂度 |

### 1.2 视觉定位

- **风格**: 现代工程工具风格（参考 VS Code、Figma、JetBrains）
- **色调**: 深色主题为主（护眼、专业），浅色主题可选
- **图标**: 线性图标风格，统一 20px/24px 规格
- **字体**: 等宽字体用于代码，UI 使用 Inter/System UI

---

## 2. 整体布局设计

### 2.1 主界面布局结构

**M12 重构后**，工程详情页（`/projects/:projectId`）采用 **三栏 IDE 布局 + 顶部导航**：

```
┌─────────────────────────────────────────────────────────────────┐
│  顶部导航 (48px)                                                  │
├──────────┬──────────────────────────────────────┬──────────────┤
│ 工程树    │   Modeling Toolbar                   │  属性面板    │
│ (256px)  │   ┌─────────────────────────────┐    │ (320px)      │
│ Project  │   │  保存 / 模式 / Palette / AI  │    │  可折叠      │
│ Tree     │   └─────────────────────────────┘    │              │
│          │                                      │  优先级：    │
│ 工程     │   ┌─────────────────────────────┐    │  画布节点 >  │
│  └ 包    │   │  建模区                      │    │  树选中节点  │
│    ├ 包  │   │  (Canvas | Editor)           │    │              │
│    └ 视图│   │                              │    │              │
│          │   └─────────────────────────────┘    │              │
│          │                                      │              │
└──────────┴──────────────────────────────────────┴──────────────┘
```

**关键约束**：
- 单一主路由 `/projects/:projectId`，通过 `?package=:pkgId` 或 `?view=:viewId` 切换中栏内容（**互斥**）
- 三栏宽度按工程持久化（localStorage key `sysmlv2.split.${projectId}`），分隔条可拖拽
- 不再使用底部 AI 助手面板（AI 改为内嵌 `<AIGenerateModal />` 触发）
- 不再使用底部状态栏

### 2.2 顶部导航栏

**高度**: 48px  
**内容**:

```
┌─────┬──────────────────────────────────┬──────────────────────┬─────┐
│ Logo│ 文件  编辑  视图  工具  帮助    │ 🔍 搜索 (Ctrl+K)     │ 👤 │
└─────┴──────────────────────────────────┴──────────────────────┴─────┘
```

| 区域 | 内容 | 说明 |
|------|------|------|
| Logo | 系统标识 | 点击返回项目列表 |
| 菜单栏 | 文件/编辑/视图/工具/帮助 | 下拉菜单，支持快捷键提示 |
| 全局搜索 | 搜索命令、项目、元素 | Ctrl+K 唤起 |
| 用户区 | 头像、设置、通知 | 下拉菜单 |

### 2.3 左侧面板 — 工程树（ProjectTree）

**默认宽度**: 256px  
**可调整范围**: 200px - 400px

**层级**（SysML v2 实体映射）：

```
工程 (Project)             ← 域容器，非 SysML
└── 包 (Package)            ← SysML 唯一命名空间实体，可嵌套
    ├── 子包 (递归)
    └── 视图 (View)         ← SysML v2 一等公民 (≈ ViewDefinition)
```

**视觉**：
- 折叠/展开 chevron + 图标（工程：`FolderTree`/包：`Package`/视图：`Eye`）
- 当前选中节点高亮 brand 色
- 视图右侧加 `colorTag` 色点（最多 8 种）
- 拖拽分隔条可调整宽度；通过工具栏按钮或 `Ctrl+B` 收起

**交互**：
- 单击：选中节点，中栏加载对应 Package/View 内容
- 双击：重命名
- 右键：唤起 `<ContextMenu />`（M12 自定义实现，非浏览器默认）
- 键盘导航：`↑ ↓ ← → Home/End Enter/Space F2 Delete`（WAI-ARIA roving tabindex）

### 2.4 主工作区

**核心区域**，根据当前模式显示不同内容：
- 文本编辑器 (Monaco)
- 图形画布 (React Flow)
- 混合视图 (左右/上下分屏)

### 2.5 右侧面板 — 属性面板（RightPane）

**默认宽度**: 320px  
**可调整范围**: 240px - 480px

**内容源优先级**：
1. 画布选中节点 → `<ElementFormPanel />`（SysML v2 元素属性编辑）
2. 树选中包节点 → `<PackagePropertiesForm />`（name/description/parentPackageId/metadata K-V/删除）
3. 树选中视图节点 → `<ViewPropertiesForm />`（name/description/colorTag/renderingCategory/metadata/exposedElements 只读列表）
4. 未选中任何节点 → `<ProjectPropertiesForm />`（项目级设置）或 `<EmptyPropertiesPane />`

**交互**：
- 可通过拖拽调整宽度
- 可通过 `Ctrl+Shift+B` 切换显示/隐藏
- 画布节点选中时自动高亮对应树节点

### 2.6 AI 助手面板（M12 已移除）

AI 入口从底部面板改为：
- **Modeling Toolbar 的 "AI 生成" 按钮** → 唤起 `<AIGenerateModal />`
- **Monaco 编辑器的 `Ctrl+/` 内联** → 行级注释提示（轻量流式）

旧版本的常驻 AI 面板（320px 高）已下线。

### 2.7 底部状态栏

**高度**: 28px  
**内容**:

```
│ SysML ▼ │ 未同步 ● │ 行 42, 列 18 │ UTF-8 │ TypeScript │ AI: 就绪 │
```

| 区域 | 内容 |
|------|------|
| 模式指示 | 当前编辑模式 (Text/Graph/Mixed) |
| 同步状态 | 已保存/未同步/同步中 |
| 光标位置 | 行号、列号 |
| 编码 | UTF-8 |
| 语言 | 当前语法高亮模式 |
| AI 状态 | AI 服务连接状态 |

---

## 3. 核心视图设计

### 3.1 项目列表视图

**路由**: `/projects`

#### 3.1.1 顶部操作区

```
┌─────────────────────────────────────────────────────────────────┐
│  我的项目                              [+ 新建项目] [🔍 搜索]   │
│                                        [卡片 ▾] [排序: 最近]   │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 卡片式布局

```
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  📁 项目封面     │  │  📁 项目封面     │  │  📁 项目封面     │
│                 │  │                 │  │                 │
│  项目名称        │  │  项目名称        │  │  项目名称        │
│  ─────────────  │  │  ─────────────  │  │  ─────────────  │
│  描述：xxx       │  │  描述：xxx       │  │  描述：xxx       │
│                 │  │                 │  │                 │
│  🕐 2小时前更新  │  │  🕐 昨天        │  │  🕐 3天前       │
│  👥 3 成员      │  │  👤 私人        │  │  👤 私人        │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

**卡片信息**:
- 封面图 (可自定义或自动生成)
- 项目名称
- 项目描述 (截断显示)
- 最后更新时间
- 成员数量

#### 3.1.3 列表式布局

```
┌─────────────────────────────────────────────────────────────────┐
│ ☐ │ 项目名称        │ 描述          │ 更新       │ 成员  │ 操作│
├───┼────────────────┼───────────────┼────────────┼───────┼─────┤
│ ☐ │ 航空系统模型    │ 无人机飞控...   │ 2小时前    │ 3     │ ⋮   │
│ ☐ │ 车载系统        │ 自动驾驶架构... │ 昨天      │ 2     │ ⋮   │
│ ☐ │ 工厂自动化      │ 生产线MES...   │ 3天前     │ 1     │ ⋮   │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.4 搜索与过滤

**搜索**:
- 支持项目名称、描述、标签搜索
- 实时搜索，高亮匹配
- 搜索历史记录

**过滤**:
- 按时间 (全部/今天/本周/本月)
- 按成员 (全部/我创建的/协作的)
- 按标签

**排序**:
- 最近更新 (默认)
- 创建时间
- 名称 (A-Z)
- 项目规模

### 3.2 工程编辑器视图（核心，M12 改造）

**路由**: `/projects/:projectId?package=:pkgId` 或 `/projects/:projectId?view=:viewId`（**互斥**）

> 旧路由 `/projects/:projectId/models/:modelId` 已弃用，由 `<LegacyModelRedirect />` 自动跳转至工程页。

工程详情页即为编辑器，采用 §2.1 三栏布局。中栏根据 `?package=` 或 `?view=` 渲染不同建模区。

#### 3.2.1 建模模式

ModelingToolbar 提供模式切换，状态保存在 `useUIStore.modelingMode`（全局 UI 偏好，非视图属性）：

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| **拖拽模式** | Palette dropdown + Canvas 拖拽创建节点 | 图形化设计、架构展示 |
| **文本模式** | 全屏 Monaco 编辑器 | 代码编写、大量文本输入 |

**模式切换**:
- ModelingToolbar 单选按钮
- 快捷键 `Ctrl+1/2`

#### 3.2.2 文本编辑器区域 (Monaco)

```
┌─────────────────────────────────────────────────────────────────┐
│ 📄 Model.sysml │ ✕                      [≡] [▢] [—]             │
├────┬──────────────────────────────────────────────────────────────┤
│  1 │ package Vehicles {                                          │
│  2 │   import Foundations::*;                                    │
│  3 │                                                              │
│  4 │   part def Vehicle;                                          │
│  5 │   part def Car :> Vehicle;                                  │
│  6 │   part def Motorcycle :> Vehicle;                          │
│  7 │                                                              │
│  8 │   // ⚡ AI 建议: 添加抽象属性                                │
│  9 │   abstract part def Vehicle {                                │
│ 10 │     attribute speed: Real;                                  │
│ 11 │   }                                                          │
│    │                                                              │
├────┴──────────────────────────────────────────────────────────────┤
│ 左侧元素符号: ✓   语法检查: ✓   AI: 已连接                       │
└─────────────────────────────────────────────────────────────────┘
```

**功能特性**:
- SysMLv2 语法高亮 (使用 Monarch 语言定义)
- 实时语法验证 (红色波浪线)
- AI 内联建议 (灰色斜体标注)
- 折叠/展开代码块
- 面包屑导航
- 诊断面板 (Problems 面板)

#### 3.2.3 图形画布区域 (React Flow)

```
┌─────────────────────────────────────────────────────────────────┐
│ 🔍 Zoom: 100% │ 👁 隐藏 │ ↙ 迷你地图 │ [📷 导出]                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│     ┌──────────┐                    ┌──────────┐               │
│     │ Vehicle  │ ─────────────────▶ │   Car    │               │
│     │ (abstract)│                    └──────────┘               │
│     └──────────┘                                                  │
│          │                                                        │
│          ▼                                                        │
│     ┌──────────┐                    ┌──────────┐               │
│     │Motorcycle│                    │  Truck   │               │
│     └──────────┘                    └──────────┘               │
│                                                                 │
│  ┌─────┐                                                      │
│  │工具栏│  ↖ 拖拽  🔗 连接  📝 注释  🎯 锚点                    │
│  └─────┘                                                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**功能特性**:
- 拖拽创建元素
- 自动布局 (Dagre 算法)
- 连线/关系绘制
- 元素分组 (Package)
- 迷你地图
- 缩放/平移
- 对齐辅助线
- 网格吸附

#### 3.2.4 混合模式布局

```
┌────────────────────────────┬────────────────────────────────────┐
│      文本编辑器             │           图形画布                 │
│      (Monaco)              │         (React Flow)               │
│                            │                                    │
│  package Vehicles {        │   ┌────────┐     ┌────────┐       │
│    part def Vehicle;       │   │Vehicle │────▶│  Car   │       │
│    part def Car :> Vehicle;│   └────────┘     └────────┘       │
│                            │       │                             │
│    // 👆 选中 2-5 行      │       ▼                             │
│                            │   ┌────────┐                       │
│                            │   │Motorc..│                       │
│  [同步高亮] ────────────────▶│   └────────┘                       │
│                            │                                    │
│  Ctrl+S 保存  Ctrl+/ AI   │                                    │
└────────────────────────────┴────────────────────────────────────┘
│  📊 同步状态: 已同步 │ 行 5 │ UTF-8 │ SysMLv2                    │
└─────────────────────────────────────────────────────────────────┘
```

**同步机制**:
- 文本修改 → 自动更新图形位置
- 图形拖拽 → 自动更新文本坐标
- 冲突时提示用户选择

#### 3.2.5 顶部工具栏

```
┌─────────────────────────────────────────────────────────────────┐
│ ← │ [📁▼] Vehicles.sysml │ │ 🔄同步 │ [文本▾] │ 💾 │ 🔗 │ ⚙ │   │
└─────────────────────────────────────────────────────────────────┘
```

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| 返回 | 返回项目列表 | Alt+← |
| 文件选择器 | 切换文件 | Ctrl+P |
| 同步 | 强制同步文本/图形 | Ctrl+Shift+S |
| 模式切换 | Text / Graph / Mixed | Ctrl+1/2/3 |
| 保存 | 保存当前文件 | Ctrl+S |
| 分享 | 协作链接 | Ctrl+Shift+C |
| 设置 | 文件设置 | Ctrl+, |

#### 3.2.6 左侧元素面板

**切换标签**: 项目文件 / SysML 元素 / 大纲

**SysML 元素面板**:

```
┌────────────────────────────────┐
│ 📦 包 (Package)         [+]   │
│ ├ 📦 Foundations             │
│ ├ 📦 Types                   │
│ └ 📦 Vehicles                │
│                               │
│ 🔷 类 (Part)                 │
│ ├ 🟦 Vehicle                 │
│ ├ 🟦 Car                     │
│ └ 🟦 Motorcycle              │
│                               │
│ 🔗 连接器 (Connector)        │
│ ├ ─ Vehicle → Car            │
│ └ ─ Vehicle → Motorcycle     │
│                               │
│ 📝 注释 (Comment)            │
│                               │
│ ⚡ 值类型 (Value Type)       │
│                               │
│ 📊 约束 (Constraint)         │
└────────────────────────────────┘
```

**元素拖拽**:
- 拖拽到图形画布创建可视化元素
- 拖拽到文本编辑器插入代码片段

### 3.3 工程编辑器 — 顶部 ModelingToolbar

ModelingToolbar 横跨中栏顶部，提供：

```
┌─────────────────────────────────────────────────────────────────┐
│ [←] │ 📄 <entity name> │ 💾保存 │ ↺撤销 │ [拖拽▾文本] │ ⋮Palette│ 🤖AI生成 │ 📋模板 │ ⬇导出 │
└─────────────────────────────────────────────────────────────────┘
```

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| 返回 | 切换到工程根节点（清空 `?package` / `?view`） | Alt+← |
| 实体名 | 展示当前包/视图名，点击重命名 | F2 |
| 保存 | 保存当前会话（带版本号乐观锁） | Ctrl+S |
| 模式切换 | `drag` / `text`（持久化到 UIStore） | Ctrl+1/2 |
| Palette | 下拉显示 14 种 SysML 元素类型（M11 解耦 viewType） | — |
| AI 生成 | 唤起 `<AIGenerateModal />` 描述即生成 SysML 文本 | Ctrl+G |
| 模板 | 唤起 `<TemplateChooserModal />` 选择模板市场条目 | — |
| 导出 | 下载当前包/视图为 `.sysml` 或 `.json` | — |

视图模式下额外提供 "暴露元素"指示器（从后端 `exposedElements` 缓存读取），用 `<Eye />` 图标展示该视图引用的元素数量。

### 3.4 AI 助手面板

#### 3.3.1 面板布局

```
┌─────────────────────────────────────────────────────────────────┐
│ 🤖 AI 助手                                      [_] [□] [×]      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 👋 欢迎！我是你的 SysML 建模助手。                         │  │
│  │                                                         │  │
│  │ 我可以帮助你：                                           │  │
│  │ • 生成 SysML 模型代码                                    │  │
│  │ • 解释现有模型逻辑                                       │  │
│  │ • 检测模型问题并提供修复建议                             │  │
│  │ • 生成图形化视图                                         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 你：帮我创建一个简单的车辆继承结构                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 🤖 AI：```sysml                                          │  │
│  │ package Vehicles {                                       │  │
│  │   abstract part def Vehicle {                           │  │
│  │     attribute speed: Real;                              │  │
│  │   }                                                      │  │
│  │   part def Car :> Vehicle {                             │  │
│  │     attribute brand: String;                            │  │
│  │   }                                                      │  │
│  │ }```                                                     │  │
│  │                                                         │  │
│  │ [插入到编辑器]  [生成图形]  [复制代码]                   │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ 📌 快捷命令                                                     │
│ ├ 💡 补全代码      │ 📖 解释选中      │ 🔍 分析模型             │
│ └ 🎨 生成图形      │ 🐛 检查错误      │ 📝 添加文档             │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────┐ [发送]                     │
│ │ 输入消息...                     │                            │
│ └─────────────────────────────────┘                            │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.3.2 快捷命令

| 命令 | 描述 | 图标 |
|------|------|------|
| 补全代码 | AI 自动补全当前代码 | 💡 |
| 解释选中 | 解释选中的代码片段 | 📖 |
| 分析模型 | 分析模型完整性 | 🔍 |
| 生成图形 | 根据代码生成图形 | 🎨 |
| 检查错误 | 检查语法和逻辑错误 | 🐛 |
| 添加文档 | 生成注释文档 | 📝 |

#### 3.3.3 生成建议气泡

```
      ┌────────────────────────────────────────┐
      │ 💡 建议：添加 speed 属性以完善 Vehicle │
      │ ──────────────────────────────────────  │
      │ abstract part def Vehicle {           │
      │   attribute speed: Real;   ← 【采纳】  │
      │ }                                    │
      │                   [忽略] [全部采纳]     │
      └────────────────────────────────────────┘
```

---

## 4. 交互流程设计

### 4.1 新建模型流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        新建项目/模型流程                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: 入口选择                                                │
│ ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐   │
│ │   📄 空白模型    │  │  📋 从模板创建  │  │  🤖 AI 生成    │   │
│ │   从零开始      │  │   选择预设模板  │  │   描述需求即可 │   │
│ └─────────────────┘  └─────────────────┘  └─────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
         【空白模型】    【模板选择】      【AI 生成】
              │               │               │
              ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: 基本信息                                                │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 项目名称: [________________________________]                │ │
│ │ 项目描述: [________________________________]                │ │
│ │ 初始包名: [________________________________]                │ │
│ │ 保存位置: [__________________] [浏览...]                   │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: 初始化配置 (可选)                                        │
│ □ 包含基础 Foundations 包                                        │
│ □ 启用实时语法检查                                               │
│ □ 启用 AI 辅助                                                   │
│ □ 设置自动保存间隔: [5分钟 ▾]                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  ✅ 创建完成     │
                    │  正在打开...     │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  编辑器视图      │
                    └─────────────────┘
```

### 4.2 编辑→保存流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        编辑-保存流程                             │
└─────────────────────────────────────────────────────────────────┘

  编辑操作
     │
     ▼
┌─────────────┐     否      ┌─────────────────┐
│ 内容变化？  │──────────▶ │ 等待下一个操作   │
└─────────────┘            └─────────────────┘
     │
    是
     │
     ▼
┌─────────────┐
│ 修改状态    │ ──▶ 显示 "● 未同步" 状态
│ 标记为脏    │
└─────────────┘
     │
     ▼
┌─────────────┐     自动保存定时器 (5分钟)
│ 自动保存？  │────────────────────────▶ 保存到云端
└─────────────┘                              │
     │                                       ▼
     │                              ┌─────────────┐
     │                              │ 状态更新    │ ──▶ 显示 "✓ 已同步"
     │                              │ 清除脏标记  │
     │                              └─────────────┘
     │
     ▼
┌─────────────┐     Ctrl+S
│ 手动保存？  │────────────────────────▶ 立即保存
└─────────────┘
     │
     ▼
┌─────────────┐     保存成功              ┌─────────────┐
│ 显示提示    │──────────────────────────▶│ 本地版本    │
│ "已保存"   │                            │ 历史记录    │
└─────────────┘                            └─────────────┘
```

### 4.3 版本回退流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        版本回退流程                              │
└─────────────────────────────────────────────────────────────────┘

打开编辑器 → 右上角 [⏱ 历史] → 弹出历史面板

┌─────────────────────────────────────────────────────────────────┐
│ 版本历史                                           [×]          │
├─────────────────────────────────────────────────────────────────┤
│                                                             ▲  │
│  ● 当前版本 ←───────────────────────────────────────        │   │
│  │ 时间: 今天 15:30                                       │   │
│  │ 操作: 编辑 Vehicle 定义                                 │   │
│  │                                                         │   │
│  │ ┌─────────────────────────────────────────────────┐    │   │
│  │ │ VS  比较                                            │    │   │
│  │ └─────────────────────────────────────────────────┘    │   │
│  │                                                         │   │
│  ○ v3  今天 14:20                                          │   │
│  │ 操作: 添加 Car 类                                      │   │
│  │                                                         │   │
│  ○ v2  今天 10:45                                          │   │
│  │ 操作: 导入 Foundations 包                               │   │
│  │                                                         │   │
│  ○ v1  今天 09:00                                          │   │
│    操作: 创建项目                                           │   │
│                                                             │   │
├───────────────────────────────────────────────────────────────┼─│
│                        │ [预览] │ [恢复此版本] │ [取消]       │ │
└─────────────────────────────────────────────────────────────────┘

点击 [恢复此版本] → 确认弹窗 → 确认回退 → 自动保存 → 刷新编辑器
```

### 4.4 协作编辑流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        协作编辑流程                              │
└─────────────────────────────────────────────────────────────────┘

发起协作:
  点击 [分享] → 生成协作链接 → 设置权限 (只读/编辑) → 发送链接

协作状态指示:
┌─────────────────────────────────────────────────────────────────┐
│ 👤 NiWinHao (你)   正在编辑第 12 行      ● 绿色                  │
│ 👤 Alice           正在查看              ○ 蓝色                  │
│ 👤 Bob             正在编辑第 45 行      ● 橙色                  │
└─────────────────────────────────────────────────────────────────┘

光标/选区共享:
  其他人的编辑位置和选区会实时显示
  不同用户用不同颜色标识

冲突处理:
  同一行同时编辑 → 提示冲突 → 选择保留版本
```

### 4.5 模板创建/使用流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        模板流程                                  │
└─────────────────────────────────────────────────────────────────┘

创建模板:
  项目 → 另存为模板 → 填写模板信息 → 选择包含内容 → 保存模板

┌─────────────────────────────────────────────────────────────────┐
│ 另存为模板                                         [×]           │
├─────────────────────────────────────────────────────────────────┤
│ 模板名称: [________________________________]                    │
│ 模板描述: [________________________________]                    │
│ 分类标签: [_____] (如: 航空、汽车、工业)                         │
│ 封面图片: [上传图片] 或 [使用自动生成]                           │
│                                                             ▲  │
│ ☑ 包含基础包定义                                              │   │
│ ☑ 包含示例模型                                               │   │
│ ☑ 包含样式配置                                               │   │
├───────────────────────────────────────────────────────────────┼─│
│                                  │ [取消] │ [保存为模板]        │ │
└─────────────────────────────────────────────────────────────────┘

使用模板:
  新建项目 → 选择模板 → 预览模板内容 → 确认使用 → 自动填充
```

---

## 5. 组件体系

### 5.1 基础组件列表

| 组件名 | 说明 | 状态 |
|--------|------|------|
| **Button** | 按钮组件 | default, hover, active, disabled, loading |
| **IconButton** | 图标按钮 | 同 Button |
| **Input** | 文本输入框 | default, focus, error, disabled |
| **Textarea** | 多行文本框 | 同 Input |
| **Select** | 下拉选择 | closed, open, disabled |
| **MultiSelect** | 多选下拉 | 同 Select |
| **Checkbox** | 复选框 | unchecked, checked, indeterminate, disabled |
| **RadioGroup** | 单选组 | selected, unselected, disabled |
| **Switch** | 开关 | off, on, disabled |
| **Slider** | 滑块 | default, dragging, disabled |
| **Modal** | 模态框 | open, closing |
| **Dialog** | 对话框 | open, closing |
| **Drawer** | 抽屉 | open, closing, left, right |
| **Popover** | 弹出层 | open, closing |
| **Tooltip** | 提示气泡 | hidden, visible |
| **DropdownMenu** | 下拉菜单 | closed, open |
| **ContextMenu** | 右键菜单 | closed, open |
| **Tabs** | 标签页 | active, inactive |
| **TabList** | 标签列表 | - |
| **Tab** | 标签 | selected, unselected |
| **Accordion** | 手风琴 | collapsed, expanded |
| **Collapsible** | 可折叠 | collapsed, expanded |
| **Separator** | 分隔线 | - |
| **Divider** | 分割器 | - |
| **Badge** | 徽章 | info, success, warning, error |
| **Tag** | 标签 | default, removable |
| **Avatar** | 头像 | with-image, initials, fallback |
| **Skeleton** | 骨架屏 | loading |
| **Spinner** | 加载动画 | - |
| **Progress** | 进度条 | determinate, indeterminate |
| **Toast** | 消息提示 | entering, visible, exiting |
| **Alert** | 警告提示 | info, success, warning, error |

### 5.2 业务组件列表

#### 5.2.1 项目相关

| 组件名 | 说明 |
|--------|------|
| **ProjectCard** | 项目卡片 (卡片视图) |
| **ProjectRow** | 项目行 (列表视图) |
| **ProjectList** | 项目列表容器 |
| **ProjectFilters** | 项目过滤栏 |
| **ProjectSearch** | 项目搜索框 |
| **NewProjectModal** | 新建项目弹窗 |

#### 5.2.2 编辑器相关

| 组件名 | 说明 |
|--------|------|
| **MonacoEditor** | Monaco 编辑器封装 |
| **EditorToolbar** | 编辑器工具栏 |
| **EditorTabs** | 多文件标签栏 |
| **EditorStatusBar** | 编辑器状态栏 |
| **BreadcrumbNav** | 面包屑导航 |
| **SyntaxHighlighter** | 语法高亮组件 |
| **CodeBlock** | 代码块展示 |

#### 5.2.3 图形画布相关

| 组件名 | 说明 |
|--------|------|
| **DiagramCanvas** | React Flow 画布容器 |
| **DiagramToolbar** | 画布工具栏 |
| **MiniMap** | 迷你地图 |
| **DiagramNode** | 图形节点 (通用) |
| **SysMLBlockNode** | SysML Block 节点 |
| **SysMLPartNode** | SysML Part 节点 |
| **SysMLPackageNode** | SysML Package 节点 |
| **DiagramEdge** | 图形连线 |
| **ConnectorLine** | 连接器线 |
| **DependencyLine** | 依赖关系线 |
| **GeneralizationLine** | 泛化线 |
| **AssociationLine** | 关联线 |
| **ElementPalette** | 元素面板 |
| **PropertiesPanel** | 属性面板 |
| **NodeInspector** | 节点检查器 |

#### 5.2.4 元素树相关

| 组件名 | 说明 |
|--------|------|
| **ElementTree** | 元素树形结构 |
| **TreeNode** | 树节点 |
| **TreeNodeIcon** | 树节点图标 |
| **TreeNodeLabel** | 树节点标签 |
| **TreeNodeActions** | 树节点操作按钮 |
| **TreeSearch** | 树搜索框 |
| **TreeFilter** | 树过滤器 |

#### 5.2.5 AI 相关

| 组件名 | 说明 |
|--------|------|
| **AIAssistantPanel** | AI 助手面板容器 |
| **AIChatMessage** | 聊天消息 |
| **AIUserMessage** | 用户消息 |
| **AIAssistantMessage** | AI 消息 |
| **AISuggestionBubble** | AI 建议气泡 |
| **AIQuickCommands** | 快捷命令列表 |
| **AIInput** | AI 输入框 |
| **AIStatusIndicator** | AI 状态指示器 |

#### 5.2.6 版本历史相关

| 组件名 | 说明 |
|--------|------|
| **HistoryPanel** | 历史面板 |
| **HistoryItem** | 历史记录项 |
| **DiffViewer** | 差异对比视图 |
| **VersionPreview** | 版本预览 |

### 5.3 组件状态定义

#### 5.3.1 Button 状态

```typescript
type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger';
type ButtonSize = 'sm' | 'md' | 'lg';
type ButtonState = 'default' | 'hover' | 'active' | 'focus' | 'disabled' | 'loading';

// 示例状态
interface ButtonProps {
  variant: ButtonVariant;  // 'primary'
  size: ButtonSize;         // 'md'
  state: ButtonState;       // 'default'
  loading?: boolean;
  disabled?: boolean;
  leftIcon?: ReactNode;
  rightIcon?: ReactNode;
}
```

#### 5.3.2 Input 状态

```typescript
type InputState = 'default' | 'focus' | 'filled' | 'error' | 'disabled' | 'readonly';

interface InputProps {
  state: InputState;
  error?: string;
  hint?: string;
  label?: string;
  prefix?: ReactNode;
  suffix?: ReactNode;
  clearable?: boolean;
}
```

#### 5.3.3 Modal 状态

```typescript
type ModalState = 'closed' | 'opening' | 'open' | 'closing';

interface ModalProps {
  state: ModalState;
  title?: string;
  description?: string;
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'full';
  closeOnOverlayClick?: boolean;
  closeOnEscape?: boolean;
}
```

---

## 6. 状态指示设计

### 6.1 加载状态

#### 全局加载

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                        ⏳ 加载中...                              │
│                                                                 │
│                     [████████░░░░] 67%                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 骨架屏 (Skeleton)

```
┌─────────────────────────────────┐
│  ┌───┐                          │
│  │照片│ 项目名称                 │
│  └───┘ ████████████             │
│        ██████                   │
│        🕐 ██████                │
│        👥 ████                  │
└─────────────────────────────────┘
```

#### 局部加载 (Inline Spinner)

```
加载中... [▌]
```

### 6.2 错误状态

#### 表单错误

```
┌─────────────────────────────────────┐
│ 用户名                               │
│ ┌─────────────────────────────────┐ │
│ │ 输入用户名...                    │ │
│ └─────────────────────────────────┘ │
│ ⚠️ 用户名已存在，请选择其他名称      │
└─────────────────────────────────────┘
```

#### 全局错误

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                      ⚠️ 出错了                                   │
│                                                                 │
│                   网络连接失败，请检查网络设置                    │
│                                                                 │
│                   [重试]  [查看详情]  [返回首页]                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 编辑器错误

```
┌─────────────────────────────────────┐
│ ⚠️ 语法错误                          │
│                                     │
│ 行 5: 意外的 token ';'              │
│   4 │   part def Car {             │
│ > 5 │     attribute;               │  ← 错误位置
│   6 │   }                          │
│                                     │
│ 💡 提示：attribute 需要指定类型      │
└─────────────────────────────────────┘
```

### 6.3 空状态

#### 项目列表空状态

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                        📁 还没有项目                              │
│                                                                 │
│                   创建你的第一个 SysML 项目                       │
│                                                                 │
│                   [🚀 创建新项目]                                │
│                                                                 │
│  ─────────── 或者从模板开始 ───────────                         │
│                                                                 │
│                   [📋 查看模板库]                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 搜索结果空状态

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                        🔍 未找到结果                              │
│                                                                 │
│                   没有找到与 "xxx" 匹配的项目                    │
│                                                                 │
│                   • 检查拼写是否正确                              │
│                   • 尝试使用更通用的关键词                        │
│                   • 清除搜索条件                                  │
│                                                                 │
│                   [清除搜索]                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 元素树空状态

```
┌─────────────────────────────────────┐
│                                     │
│         📭 模型为空                   │
│                                     │
│   开始添加 SysML 元素：              │
│   • 在编辑器中编写代码              │
│   • 或从元素面板拖拽元素             │
│                                     │
└─────────────────────────────────────┘
```

### 6.4 成功提示

#### Toast 提示

```
┌─────────────────────────────────┐
│ ✓ 操作成功                      │
│   文件已保存                    │
└─────────────────────────────────┘
          ↓ (3秒后自动消失)
```

#### 确认操作

```
┌─────────────────────────────────────────────────────────────────┐
│                        ✓ 确认操作                                │
│                                                                 │
│             确认要删除项目 "航空系统模型" 吗？                    │
│             此操作无法撤销。                                     │
│                                                                 │
│             [取消]                           [确认删除]           │
└─────────────────────────────────────────────────────────────────┘
```

### 6.5 状态指示器汇总

| 场景 | 视觉表现 |
|------|----------|
| 保存成功 | Toast: 绿色勾号 + "已保存" |
| 保存失败 | Toast: 红色警告 + "保存失败，请重试" |
| 同步中 | 状态栏: 旋转图标 + "同步中..." |
| AI 生成中 | 输入框: 加载动画 + "AI 思考中..." |
| 上传中 | 进度条 + 百分比 |
| 网络断开 | 顶部红色横幅 + "网络连接已断开" |
| 权限不足 | 弹窗: "您没有权限执行此操作" |

---

## 7. 快捷键设计

### 7.1 全局快捷键

| 快捷键 | 功能 | 范围 |
|--------|------|------|
| `Ctrl+K` | 全局搜索 | 全局 |
| `Ctrl+S` | 保存 | 编辑器 |
| `Ctrl+Shift+S` | 另存为 | 编辑器 |
| `Ctrl+Z` | 撤销 | 编辑器 |
| `Ctrl+Y` / `Ctrl+Shift+Z` | 重做 | 编辑器 |
| `Ctrl+C` | 复制 | 全局 |
| `Ctrl+V` | 粘贴 | 全局 |
| `Ctrl+X` | 剪切 | 全局 |
| `Ctrl+A` | 全选 | 全局 |
| `Ctrl+F` | 查找 | 编辑器 |
| `Ctrl+H` | 替换 | 编辑器 |
| `Ctrl+G` | 跳转到行 | 编辑器 |
| `Ctrl+B` | 切换左侧面板 | 全局 |
| `Ctrl+Shift+B` | 切换右侧面板 | 全局 |
| `Ctrl+/` | 打开 AI 助手 | 全局 |
| `Ctrl+1` | 切换到拖拽模式 | 工程编辑器 |
| `Ctrl+2` | 切换到文本模式 | 工程编辑器 |
| `Ctrl+G` | AI 生成 SysML 文本 | 工程编辑器 |
| `Ctrl+P` | 快速打开文件 | 全局 |
| `Ctrl+N` | 新建 | 全局 |
| `Ctrl+W` | 关闭当前标签 | 编辑器 |
| `Ctrl+Shift+T` | 重新打开关闭的标签 | 编辑器 |
| `Ctrl+,` | 打开设置 | 全局 |
| `F11` | 全屏 | 全局 |
| `Alt+F4` | 关闭窗口 | 全局 |

### 7.2 编辑器快捷键

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 缩进 |
| `Shift+Tab` | 取消缩进 |
| `Ctrl+D` | 选择下一个相同词 |
| `Ctrl+Shift+L` | 选择所有相同词 |
| `Ctrl+]` | 缩进 |
| `Ctrl+[` | 取消缩进 |
| `Ctrl+/` | 注释/取消注释 |
| `Ctrl+Shift+K` | 删除整行 |
| `Alt+↑` | 向上移动行 |
| `Alt+↓` | 向下移动行 |
| `Ctrl+Enter` | 在下方插入行 |
| `Ctrl+Shift+Enter` | 在上方插入行 |
| `Ctrl+Space` | 触发补全 |
| `Ctrl+Shift+F` | 格式化代码 |

### 7.3 工程树快捷键（M12 新增）

| 快捷键 | 功能 |
|--------|------|
| `↑` / `↓` | 上下节点导航 |
| `←` / `→` | 折叠 / 展开节点 |
| `Home` / `End` | 跳到首/末节点 |
| `Enter` / `Space` | 选中/激活节点 |
| `F2` | 重命名节点 |
| `Delete` | 删除节点（含确认对话框） |

### 7.4 图形画布快捷键

| 快捷键 | 功能 |
|--------|------|
| `Delete` / `Backspace` | 删除选中 |
| `Ctrl+A` | 全选 |
| `Ctrl+D` | 复制选中 |
| `Ctrl+V` | 粘贴 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |
| `Space` (拖拽) | 平移画布 |
| `Ctrl+滚轮` | 缩放 |
| `Ctrl+0` | 适应窗口 |
| `Ctrl+1` | 缩放到 100% |
| `Ctrl+Shift+E` | 居中选中 |

### 7.5 AI 助手快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+/` | 唤起 AI 助手 |
| `Enter` | 发送消息 |
| `Shift+Enter` | 换行 |
| `Escape` | 关闭助手 |
| `↑` (输入框) | 查看历史输入 |
| `↓` (输入框) | 查看下一条历史 |

---

## 8. 响应式策略

### 8.1 断点定义

| 断点 | 屏幕宽度 | 说明 |
|------|----------|------|
| Mobile | < 640px | 手机端 |
| Tablet | 640px - 1024px | 平板 |
| Desktop | 1024px - 1440px | 标准桌面 |
| Wide | > 1440px | 宽屏 |

### 8.2 桌面端布局 (≥1024px)

```
┌─────────────────────────────────────────────────────────────────┐
│ 顶部导航 (48px)                                                  │
├──────────┬────────────────────────────────────┬─────────────────┤
│ 左侧面板 │         主工作区                    │    右侧面板     │
│ (240px)  │         (flex)                    │    (280px)     │
│          │                                    │                │
│ 可折叠   │    编辑器 / 画布 / 混合视图         │    可折叠       │
│          │                                    │                │
│          ├────────────────────────────────────┤                │
│          │    AI 助手面板 (可折叠)             │                │
│          │    (320px)                         │                │
├──────────┴────────────────────────────────────┴─────────────────┤
│ 底部状态栏 (28px)                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 8.3 平板适配 (640px - 1024px)

```
┌─────────────────────────────────────────────────────────────────┐
│ 顶部导航 (48px)                                                  │
├──────────┬────────────────────────────────────┬─────────────────┤
│ 左侧面板 │         主工作区                    │                 │
│ (200px)  │         (flex)                     │                 │
│ 可折叠   │    编辑器 / 画布 / 混合视图         │    隐藏         │
│          │                                    │    (点击展开)   │
│          ├────────────────────────────────────┤                │
│          │    AI 助手面板 (可折叠)             │                │
│          │    (280px)                         │                │
├──────────┴────────────────────────────────────┴─────────────────┤
│ 底部状态栏 (28px)                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**适配策略**:
- 右侧面板默认隐藏，通过工具栏按钮展开
- AI 助手以抽屉形式从底部弹出
- 混合模式改为单视图 + 切换按钮

### 8.4 移动端适配 (< 640px)

```
┌─────────────────────┐
│ 顶部导航 (44px)     │
├─────────────────────┤
│                     │
│                     │
│    主工作区          │
│    (全屏)           │
│                     │
│                     │
├─────────────────────┤
│ 底部 Tab 栏          │
│ [项目] [编辑] [AI]  │
└─────────────────────┘
```

**适配策略**:
- 完全隐藏左右侧面板
- 提供底部 Tab 切换主要功能
- AI 助手全屏化
- 简化工具栏，只保留核心操作
- 不支持混合模式
- 图形模式仅查看，不支持复杂编辑

### 8.5 响应式组件行为

| 组件 | 桌面端 | 平板端 | 移动端 |
|------|--------|--------|--------|
| 左侧面板 | 常驻 | 可折叠 | 隐藏 |
| 右侧面板 | 常驻 | 抽屉式 | 隐藏 |
| AI 助手 | 内嵌 | 抽屉式 | 全屏 |
| 工具栏 | 完整 | 精简 | 图标 |
| 快捷键 | 完整支持 | 部分支持 | 隐藏提示 |
| 拖拽操作 | 完整支持 | 手势支持 | 禁用 |

---

## 9. 视觉规范

### 9.1 色彩系统

#### 深色主题 (默认)

```css
:root {
  /* 背景色 */
  --bg-primary: #0d1117;      /* 主背景 */
  --bg-secondary: #161b22;    /* 次级背景 */
  --bg-tertiary: #21262d;    /* 三级背景 */
  --bg-elevated: #30363d;    /* 提升层背景 */

  /* 边框色 */
  --border-default: #30363d;
  --border-subtle: #21262d;
  --border-strong: #484f58;

  /* 文字色 */
  --text-primary: #e6edf3;
  --text-secondary: #8b949e;
  --text-tertiary: #6e7681;
  --text-link: #58a6ff;

  /* 功能色 */
  --accent-primary: #58a6ff;  /* 主强调色 */
  --accent-secondary: #1f6feb;
  --success: #3fb950;
  --warning: #d29922;
  --error: #f85149;
  --info: #58a6ff;

  /* SysML 元素色 */
  --element-package: #a371f7;
  --element-block: #58a6ff;
  --element-part: #3fb950;
  --element-connector: #d29922;
  --element-constraint: #f85149;
}
```

#### 浅色主题

```css
:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f6f8fa;
  --bg-tertiary: #eaeef2;
  --bg-elevated: #ffffff;

  --border-default: #d0d7de;
  --border-subtle: #eaeef2;
  --border-strong: #afb8c1;

  --text-primary: #1f2328;
  --text-secondary: #656d76;
  --text-tertiary: #8c959f;
  --text-link: #0969da;

  --accent-primary: #0969da;
  --success: #1a7f37;
  --warning: #9a6700;
  --error: #cf222e;
  --info: #0969da;
}
```

### 9.2 字体系统

```css
:root {
  /* UI 字体 */
  --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

  /* 代码字体 */
  --font-mono: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace;

  /* 字号 */
  --text-xs: 11px;
  --text-sm: 12px;
  --text-base: 14px;
  --text-lg: 16px;
  --text-xl: 18px;
  --text-2xl: 20px;
  --text-3xl: 24px;

  /* 行高 */
  --leading-tight: 1.25;
  --leading-normal: 1.5;
  --leading-relaxed: 1.75;

  /* 字重 */
  --font-normal: 400;
  --font-medium: 500;
  --font-semibold: 600;
  --font-bold: 700;
}
```

### 9.3 间距系统

```css
:root {
  /* 基础间距单位: 4px */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;

  /* 组件间距 */
  --component-gap: var(--space-4);
  --section-gap: var(--space-6);
  --page-padding: var(--space-6);
}
```

### 9.4 圆角与阴影

```css
:root {
  /* 圆角 */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-full: 9999px;

  /* 阴影 */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.1);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.15);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.2);
  --shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.25);

  /* 过渡 */
  --transition-fast: 100ms ease;
  --transition-normal: 200ms ease;
  --transition-slow: 300ms ease;
}
```

### 9.5 图标规格

| 类型 | 尺寸 | 使用场景 |
|------|------|----------|
| Micro | 12px | 内联文字、徽章 |
| Small | 16px | 按钮内、标签页 |
| Medium | 20px | 工具栏、树节点 |
| Large | 24px | 空状态、介绍页 |
| XLarge | 32px | 欢迎页、功能入口 |

---

## 附录

### A. 技术依赖

| 库 | 版本 | 用途 |
|----|------|------|
| React | 18.x | UI 框架 |
| TypeScript | 5.x | 类型系统 |
| Radix UI | 1.x | 无样式组件库 |
| Tailwind CSS | 3.x | 样式框架 |
| Monaco Editor | 0.45.x | 代码编辑器 |
| React Flow | 11.x | 图形画布 |
| Zustand | 4.x | 状态管理 |
| React Query | 5.x | 数据获取 |
| i18next | 23.x | 国际化 |

### B. 后续迭代计划

**MVP 后 (v2.0)**:
- [ ] 协作实时编辑 (Yjs)
- [ ] 插件系统
- [ ] 自定义元素类型
- [ ] 导出功能 (PDF/SVG/PNG)
- [ ] 版本分支管理

**长期规划**:
- [ ] 团队权限管理
- [ ] 模型仿真集成
- [ ] 代码生成器
- [ ] 模型验证规则引擎

---

*文档结束*
